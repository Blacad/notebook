```
def build(*names,flag=False):
    def printed(f):
        if names:
            print(f'{names[0]}') 
        return f
    return printed
@build('test')
def test(a):
    a = a + 1
    return a


print(test(1))
```
