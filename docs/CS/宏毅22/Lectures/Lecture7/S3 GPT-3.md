
- GPT 想要的终极目标 ---  Zero-Shot
	- 渐进的目标
		- Few-shot
		- One-shot
		- Zero-shot

- GPT 的模型在 ANLI 中表现差劲
	- ANLI 就是 给模型两个句子，看两个句子是否矛盾(BERT更适合)

- 使用 GPT3 产生影像
	- 没有使用CNN和其它特殊设计，就是把图片一行行输入GPT3(rader order)，让其预测下一个像素点以此类推(但是当然GPT3是已经做过相应训练的)